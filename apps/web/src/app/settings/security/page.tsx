import React from 'react';
import { SecurityClient } from './SecurityClient';

export const metadata = {
  title: 'Security Settings - ExamForge',
};

export default function SecurityPage() {
  return <SecurityClient />;
}
